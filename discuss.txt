*块大小？
越大，小文件储存效率降低，空间浪费越大。（但目前储存的成本很低）
越小，反之。但越小会导致B树的度越小，高度越高，寻址次数越多。
4kB为文件系统常见大小
1kB为老版本使用的块大小

*.是否支持超长文件名？
如果支持，需要考虑实现方式
给目录加上特殊标记，表示其是否为一个最终节点（如果不是，则它仅仅用于延长文件名）

*.文件内部块寻址如何实现？
方案一：二进制字典树（也可看做2^n叉线段树）
方案二：二级链表

*.目录索引实现？
B+树？
B*树？

*.文件信息头包含哪些信息？

*.目录信息头包含哪些信息？

*.是否打算在以后的版本加入加密功能？
（是的话我们需要预留接口）

*.打开文件或者目录后，产生的指针和缓存信息，由主类维护还是由打开工具类维护？（主类维护类似于真正的操作系统，只对外提供一个句柄来引用内部信息，但这种方式将增加维护的时间和空间成本）

