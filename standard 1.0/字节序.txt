LKVS内部采用小端字节序(Little-Endian)。
    endian.h定义了重载的endian_convert函数，可用于任何长于1字节的整型变量，返回转化以后的字节序。
注意，endian_convert在小端字节序机器上将不会进行任何转换，而在大端字节序机器上将进行转换。所以只要在将数据写入到磁盘前，以及从磁盘读取数据后，直接调用一次endian_convert，即可统一文件内部的字节序为小端字节序。